<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".presentation.locations.CreateLocationActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        tools:ignore="UselessParent">

        <include
            layout="@layout/toolbar"/>

        <LinearLayout
            android:id="@+id/linearLayoutCreateLocation_AddChildren"
            android:layout_width="match_parent"
            android:layout_height="?android:attr/actionBarSize"
            android:background="@color/colorPrimary"
            android:elevation="6dp"
            android:orientation="horizontal"
            android:visibility="gone" >

            <TextView
                style="@style/standard_text_view"
                android:id="@+id/textViewCreateLocation_AddChildren"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:gravity="end|center_vertical"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:textColor="@color/white"
                android:layout_weight="1"/>

            <CheckBox
                android:id="@+id/checkBoxCreateLocation_AddChildren"
                android:layout_height="match_parent"
                android:layout_width="wrap_content"
                android:layout_gravity="end|center_vertical|center_horizontal"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:buttonTint="@color/white" />

        </LinearLayout>

        <EditText
            style="@style/standard_edit_text"
            android:id="@+id/editTextCreateLocation"
            android:hint="@string/hint_enter_location_name"
            tools:ignore="Autofill" />

    </LinearLayout>

</FrameLayout>